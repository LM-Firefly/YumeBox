name: Deprecated Build Entry

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is deprecated
        run: |
          echo "::warning::This workflow is deprecated. Use 'CI Channel' or 'Release Stable' workflows."
